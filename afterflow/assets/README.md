# Font assets

`fonts/` contains the five files loaded by `next/font/local`. Next.js emits
fingerprinted build assets; no duplicate copies belong in `public/`.

`licenses/` preserves the supplied Novela licence. Its OTF files are unchanged.

Original collections, trial fonts and design archives are stored outside this
repository. `assets/source-fonts/` is ignored to prevent accidental commits.
